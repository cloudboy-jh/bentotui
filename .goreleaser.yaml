version: 2

project_name: bento

before:
  hooks:
    - go mod tidy

builds:
  - id: bento
    binary: bento
    main: ./cmd/bento
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: bento
    ids: [bento]
    name_template: "bento_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: cloudboy-jh
    name: bentotui
  draft: false
  prerelease: auto
  # Allow CI to overwrite assets if they already exist.
  # This handles the case where a local goreleaser run uploaded assets
  # before CI triggered on the same tag push.
  replace_existing_artifacts: true
